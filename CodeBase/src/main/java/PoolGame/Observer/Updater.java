package PoolGame.Observer;

/**
 * Observer interface of the observer pattern
 */
public interface Updater {
    /**
     * Updates to the new info  
     * @param newinfo
     */
    public void update(String newinfo);
}
